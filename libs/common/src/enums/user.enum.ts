export enum UserRole {
  USER = 'user',
  ADMIN = 'admin',
  SELLER = 'seller',
  SUPER_ADMIN = 'super_admin',
}

export enum UserStatus {
  ACTIVE = 'active',
  INACTIVE = 'inactive',
  PENDING = 'pending',
  SUSPENDED = 'suspended',
}
